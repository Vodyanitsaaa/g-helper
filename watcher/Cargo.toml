[package]
name = "ghelper_watcher"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "GHelperWatcher"
path = "src/main.rs"

[dependencies]

[build-dependencies]
winres = "0.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
